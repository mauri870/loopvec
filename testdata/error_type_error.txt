# Packages that do not type-check are reported with the compiler's diagnostics,
# nothing is written, and the exit status is non-zero.

cp ops.go ops.go.orig
! loopvec -split .
! stdout .
stderr -count=1 '^\./ops\.go:5:19: undefined: missing$'
stderr -count=1 '^loopvec: errors occurred$'
! stderr 'rewrote'
cmp ops.go ops.go.orig
! exists ops_simd.go

-- go.mod --
module example.com/test

go 1.27.1
-- ops.go --
package test

func AddInt32s(dst, a, b []int32) {
	for i := range dst {
		dst[i] = a[i] + missing[i]
	}
}
